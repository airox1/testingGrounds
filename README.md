# Testing Grounds

## Lucky Mirage Slots

A single-page fake gambling game built with HTML/CSS/JS. Run it locally by opening
`index.html` in a browser or serving the directory with a simple web server.
